# ============================================================================
# FYP_HKBU —— 体积大 / 可再生的内容一律不进版本库
# ============================================================================
# 事故记录（2026-09-23）：根目录 .git 曾积累 76,427 个不可达松散对象 = 57 GB，
# 成因是「git add . 之后从未 commit」，把 47 GB 数据集 + 21 GB 推理产物全部
# 塞进对象库。当时 E 盘可用空间归零，所有脚本写入失败（ENOSPC）。
# `git prune --expire=now` 已回收全部 57 GB。本文件用于防止复发。

# ---- 数据集（47 GB，从上游重新下载即可） ----
02_databases/

# ---- 模型权重与第三方仓库（15 GB + 743 MB） ----
01_models/_weights/
01_models/_third_party/

# ---- 逐模型文件夹里的代码联接（目录联接，指向 _third_party） ----
# 2026-09-23 起模型按板块分类：01_models/<separation|denoising>/<模型>/code/
# 必须显式忽略：git 会把目录联接当普通目录递归进去，把 2 GB 第三方仓库再抄一遍
01_models/*/*/code/
# 旧扁平布局的残留（若迁移未彻底）
01_models/*/code/
01_models/*/weights/
# 各模型目录下的其它内容一律不追踪（里面只剩联接；说明文档在 <板块>/README.md）
01_models/*/*/

# ---- 推理产物（21 GB，各模型 test/ 下为 564 个组合的分离音频） ----
03_outputs/

# ---- 临时与缓存（2026-09-23 由 05_misc 并入 tools/） ----
tools/_scratch/
tools/_logs/
**/.cache/
**/__pycache__/
*.py[cod]

# ---- 保持追踪（体积小、是交付物） ----
!01_models/
!01_models/README.md
!01_models/MODEL_REGISTRY.md
!01_models/*/README.md
!04_reports/
!tools/
